apiVersion: v1
kind: Service
metadata:
  name: mongodb-svc
  namespace: proshop
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None  # Headless service untuk StatefulSet